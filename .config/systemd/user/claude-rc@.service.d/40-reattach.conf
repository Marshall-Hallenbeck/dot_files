[Service]
# After every (re)start, reattach sessions the previous instance dropped.
# "-" prefix: a reattach failure must not take down the daemon itself.
ExecStartPost=-%h/.local/bin/claude-rc-reattach %i
